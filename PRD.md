# yl Skill 集群：公开发行版设计

版本：1.1.0 · 状态：本地公开发行 · 日期：2026-09-15

## 目标与两份版本的关系

把经过实际使用的 Skill 整理为其他人能独立安装、选择和使用的工具箱。日常版是功能演进的来源；这里是经过脱敏、依赖整理和许可审核的公开发行版，不是日常目录的原样备份。原始备份如有需要应另存私人位置，不进入本项目。

更新方向固定为：日常来源 → 文件差异 → 公开适配 → 审核 → 版本化发行。公开版的重命名、匿名案例、相对路径和安全配置是需要保留的独立改动；禁止用整目录覆盖的方式更新，禁止反向覆盖日常版。

## 当前能力

| 分类 | 入口与能力 | 选择方式 |
| --- | --- | --- |
| 总入口 | `yl-toolbox` | 理解当前任务，选择分类或具体工具，必要时组合 |
| 写作 | `yl-writing` | 展示两种风格与公共基座编辑；契诃夫为常用首选 |
| 写作总框架 | `yl-write-voice-led-wechat` | 本人口述稿转公众号，保留思想链、查资产、可选契诃夫、暴击或自带编辑 |
| 写作成员 | `yl-chekhov-deep-writing` | 观察、人物处境、克制叙事与深层机制 |
| 写作成员 | `yl-write-impact-wechat` | 暴击／咪蒙式传播机制、冲突与观点推进 |
| 视频 | `yl-video` | 剪辑、封面或用户明确要求的组合 |
| 视频成员 | `yl-remotion-talking-head-editor` | 既有真人口播精剪工作流 |
| 视频成员 | `yl-short-video-covers` | 黑底白字／半透明无头像，两种封面模式 |
| 安装与维护 | `yl-skill-sync` | 本地映射、状态核对；自动下载升级待完善 |

Human Writing 与卡兹克不再作为独立工具；其中已审查的共通方法进入公共基座，原署名与适用许可保留。封面半透明模式可保留视频原帧中的人物，但不叠加头像或人物抠图。

## 编排原则

参考 DBS 的总入口、候选发现与主辅分工思想，重新编写 yl 路由，不复制上游联网检查或绑定其运行环境。总入口从生成目录发现能力，分类入口负责有意义的用户选择，成员保留完整执行规则。

用户已选风格或工具就继续执行；没有选择时，写作入口提供简短菜单。用户授权自行决定时结合材料选择，契诃夫优先用于人物、生活和叙事材料。一次文章只采用一个主写作风格，避免把多套全文风格叠加。需要成文、封面、视频的同一任务可分工协作；依赖顺序、真实材料与已有授权在交接中保留。

## 写作公共约束

口述稿流程管理原稿、知识与表达资产、增补依据和交付；契诃夫、暴击互斥提供主写法，也可直接做基座编辑。所有写作入口必须加载公共去 AI 味规则，不能靠换框架绕开。规则默认优先保护原话，对已确认问题修复后再交稿；关键词和密度只作为候选，不制造机器质量分。

公共原件位于 `yl-writing`，各写作成员携带相同副本，退役的 Human Writing 便携版不再生成。构建验证副本和直调入口，来源指纹帮助跟踪日常更新。运行审校记录留安装者工作区，不污染正文。

## 发行与更新

- 集群版本采用 `主版本.次版本.修订版本`。首版 `1.0.0`；兼容新增能力可为 `1.1.0`；修复为 `1.0.1`；破坏安装或调用兼容性的变化才升 `2.0.0`。
- `release.json` 保存集群和成员版本、分类、来源。`cluster.json` 保存本次发行的文件哈希。成员来源快照用于发现日常版变化。
- 更新先运行 `python tools/check_updates.py`，也可以指定其他日常 Skill 根目录或单个来源。只比对，不自动复制、不联网、不写回日常版。
- 建议每月或准备分享前手动比对一次；本项目不默认创建定时任务。
- 新增或修改的文件须完成自动敏感扫描、逐文件语义复核、来源归属及必要运行验证。审核记录绑定文件 SHA-256，变化后必须复核再登记。
- 许可按成员/材料单独登记。当前剪辑工作流、口述稿编辑方法及公共去 AI 味层包含 CC BY-NC 4.0 的 DBS 衍生材料，保留非商业限制；整包不能统一称为无限制商用的 MIT 开源包。将来要全包允许商业使用，先取得相关授权或独立重建受限部分，再审核发版。
- 构建生成带版本的 ZIP 和固定名 `yl-toolbox-cluster.zip`。已有发行号不能悄悄覆盖为不同字节，应递增版本。

## 安装与使用边界

所有 Skill 使用 `yl-` 命名；不覆盖安装者已有的不同版本。安装器先检查全部成员，再写文件。不同版本的更新由安装者在 Skill 目录外备份后操作。运行素材、凭据、音视频和缓存放在 Skill 目录之外。

各成员能单独安装；入口需要所选成员也在同级安装目录。视频生成依赖安装者的 FFmpeg、Python 包、字体和相关服务。外部依赖与费用由具体 Skill 说明，包内不携带作者账号或媒体。

## 本次范围与验收

本次 2.0.0 完成本地 9 个 Skill 的精简、写作基座补充、维护文档与安装包验证。GitHub 仓库已存在，本轮不推送或发布；宣传图待确认。自动更新链路待完善，不视为已完成能力。

验收对象包括：入口可找到实际成员；风格与封面模式可选择；相对引用有效；公开成员保留完整核心方法；无未解释敏感信息；第三方许可保留；比对能识别来源变化；安装预检不写入；相同版本幂等；不同版本拒绝覆盖；ZIP 解压文件逐字节一致。结构测试不代表实际文章质量或完整视频已验证。
