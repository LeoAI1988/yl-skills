# v2.1.0 日常来源对齐报告

核对日期：2026-09-21。前版 v2.0.2 为 9 项，本版为 10 项。只修改公开库，Codex／WorkBuddy 的日常源与映射目标均只读。

## 逐项结论

| 工具 | 来源与结论 | 本次处理 |
|---|---|---|
| 工具箱总入口 | 本库维护的路由 | 加入视频号数据复盘；生成目录登记全部成员 |
| 写作入口与基座 | Human Writing、卡兹克与已登记的 DBS 规则源 | 复核新增／变化；日常版公共约束回写已被公开版覆盖，保留语义裁决和原话保护，不重复抽取或恢复退役成员 |
| 口述公众号 | Codex 与 WorkBuddy 指向同一份日常源 | 补旧 `dbs` 框架名到内置 `editorial` 的兼容说明，以及 `--no-cta`；核心保真、增补核对、终审已具备，不冒称新增写作算法 |
| 契诃夫 | 日常源机制、语言外壳和工作流与公开版一致 | 保留公开语料、许可及公共约束；来源快照补充回写后文件，未增添中文译本或全集 |
| 暴击观点 | 主要差异为公共约束回写、私人署名／CTA 和环境工具绑定 | 公开版已有等价共通方法；保留安装者可配置结尾和可选连接器 |
| 视频入口 | 本库维护的路由 | 现有剪辑／双模式封面路由足够，不新增重复入口 |
| 口播自动剪辑 | 日常剪辑规则新增前五秒细查、转写覆盖、成片复审及回片处理 | 合并方法并独立改写复审脚本，修正来源脚本近重复排序、字幕定位、失败检测等问题；增加 VAD/ASR 差集助手 |
| 短视频封面 | 大字源入口变化，多数细节已在公开版完整参考中；半透明源未变 | 保留现有标题补充、绿幕和遮挡方法，强化人像位于文字上层与完整头部检查 |
| 安装与维护 | 本库的公开适配版 | 保留现有安全限制，没有把未修复的 URL 更新和 Grok 适配当作可用升级能力 |
| 视频号数据采集与复盘 | Codex 平台分析＋WorkBuddy 视频号后台＋WorkBuddy 小店采集 | 整合为 `yl-channels-analytics`；移除私人案例，保留完整专业方法；独立 CSV 统计避免依赖原作者的私有仓库结构 |

## 视频更新的执行含义

- 前五秒从 0.08 秒起检查无信息间隙，确认后总保留约 0.05 秒，按成片坐标换算源时间；完整字音和有意义语气仍要保留。
- 剪前比较 VAD 与 ASR 区间，漏覆盖达到 0.30 秒进入听审／局部转写，不能当静音删除。
- 剪后生成重复、填充、静音与字幕冻结候选，逐项裁决并绑定实际成片哈希；失败或零候选均不等于完成主观验收。
- 删除有声字词要同步音频、字幕和成片稿；临时手改写回编辑真源；保护已有用户成品。
- 增强回片先查像素方向与旋转元数据，按真实源质量处理比例、帧率及 HDR，不一刀切到固定竖屏规格。

## 公开适配中的保留与排除

保留有效规则、完整操作方法、相对引用、必要脚本、原有作者归属和混合许可。排除私人姓名／账号、联系方式、真实经营和订单案例、作者本机绝对路径、凭据、原片及专属仓库写入流程。后台日期限制、接口字段和选择器作为历史线索，须每次核实，不能包装成平台永久承诺。

未把日常版固定 1800 字上限、固定推广结尾、必须具备作者知识库、旧卡兹克／Human Writing 风格菜单强制给所有安装者。写作公共层继续保护原话和有效修辞；工具词表不是硬清零规则。

## 来源追踪

各成员 `references/source-snapshot.json` 登记本次已核对文件的来源指纹。`unchanged` 表示来源自本轮核对后未再变化，不表示公开版与来源字节相同。公开改编的理由以上表为准。

契诃夫旧快照中的 `chekhov-deep-writing-release` 是历史工程来源，本次默认来源目录中不存在，已保留于 `historical_sources` 并明确标为不可用；当前日常 `chekhov-deep-writing` 已单独核对，不冒充检查了已缺失的历史工程。旧语料的既有审查和许可不因此改写。

WorkBuddy 的两项源使用 `host: workbuddy` 定位，其他日常源默认 Codex；可用 `tools/check_updates.py --source 名称=安装者自己的路径` 显式覆盖。来源脚本不会自动复制或改写日常版。

## 验证与能力边界

新增脚本以合成字幕、区间和播放数据验证：字幕页定位、七字重复／近重复、音频失败、覆盖差集、快照去重、缺值、混合口径拒绝、分布与原文件保护。写作基座与口述稿已有回归继续执行；发行需通过结构、引用、敏感扫描、包字节一致和临时安装验证。

本轮未登录真实后台、未重新剪真人视频，不能据静态审查和合成测试声称真实采集或视听验收通过。旧宣传图像素不变，README 标为历史版本，当前 10 项以新清单和生成目录为准。
