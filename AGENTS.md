# yl Skill 集群维护规范

本目录是准备分享给学员的精选 Skill 工具箱，所有可安装 Skill 的目录名、SKILL.md 的 name、调用提示均须使用 `yl-` 前缀。

新增或更新时：

1. 保留完整方法、必要脚本和合法的来源归属；不得包含私人身份、联系方式、凭证、账户、私有路径、真实项目记录或媒体。
2. 做自动敏感信息扫描及逐文件语义复核；扫描无命中不等于完成审核。凭证由安装者自行配置，运行数据放在 Skill 目录之外。
3. 修改仅作用于本分享库；不要反向覆盖个人正在使用的原版 Skill。
4. Skill 采用顶层 `yl-功能名称/`，frontmatter 的 name 与目录名相同，description 清楚描述适用任务。每项可独立调用，工具箱只负责路由，不替代具体规则。
5. 更新 `yl-toolbox/references/catalog.json`，确保目录包含所有已审核 Skill；可运行 `python build.py` 自动生成目录和集群包。新增工具必须先完成审核再打包，构建脚本不是人工审核的替代品。
6. 验证相对引用、脚本、安装行为及 ZIP 字节一致性。默认不覆盖安装者已有的不同版本。
7. 只分享生成的 `yl-toolbox-cluster.zip`，或单独的已审核 Skill 目录。未经用户授权不发布到外部平台。

集群新成员不要依赖作者机器已有的文件、凭证或其他未列明的私人 Skill。第三方 SDK、字体、模型按许可处理，不能因增加 yl 前缀而抹去原作者归属。
